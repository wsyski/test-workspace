package com.axiell.arena.liferay.modules.template_contexts.api;

import com.liferay.dynamic.data.mapping.model.DDMStructure;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;

public class AssetFactoryFactoryTest {

    @Mock
    DDMStructure struct1Mock;

    @Mock
    DDMStructure struct2Mock;

    @Test
    public void testFactory() {
//        when(struct1Mock.getStructureKey()).thenReturn("76543");
//        when(struct1Mock.getName(any(Locale.class))).thenReturn("Branch Information");
//        when(struct2Mock.getStructureKey()).thenReturn("51435");
//        when(struct2Mock.getName(any(Locale.class))).thenReturn("Staff pick");
//        IAssetFactoryBuilderFactory factory = new AssetFactoryBuilder.Factory(Arrays.asList(struct1Mock, struct2Mock),
//                null, null, null);
//        assertThat(factory, is(notNullValue()));
//        Map<String, Class<? extends IModelFactory>> structureMap = factory.getStructureMap();
//        assertThat(structureMap, is(notNullValue()));
//        assertThat(structureMap.size(), is(2));
//        assertThat(structureMap.keySet(), hasItem("51435"));
//        assertThat(structureMap.keySet(), hasItem("76543"));
//        assertThat(structureMap.get("51435"), equalTo(StaffPickFactory.class));
//        assertThat(structureMap.get("76543"), equalTo(LibraryBranchFactory.class));
    }

}
