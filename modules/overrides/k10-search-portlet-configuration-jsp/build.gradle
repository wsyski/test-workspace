dependencies {
}

publishing {
	publications {
		mavenJava(MavenPublication) {
			artifactId 'com.axiell.arena.liferay.modules.overrides.k10_search_portlet.configuration.jsp'
			groupId "${mavenGroup}"
			version "${mavenVersion}"
			from components.java
		}
	}
}

